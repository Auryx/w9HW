<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head.ejs')%>
<body>
    <%- include('../partials/header.ejs')%>
    <main>
        <div>
            <article>
                <h2><%= animal.species %> - <%= animal.extinct ? "Extinct" : "Not Extinct" %></h2>
                <h3>Found in <%= animal.location %></h3>
                <h3>Life Expectancy: <%= animal.lifeExpectancy%> years</h3>
                <a href="/animals/<%= animal._id%>/edit"><button>edit</button></a>
                <form action="/animals/<%=animal._id%>?_method=Delete" method="POST">
                    <input type="submit" value="Delete">
                </form>
                <a href="/animals"><button>Back to Main Menu</button></a>
            </article>
        </div>
    </main>
</body>
</html>